# 複製這個檔案成 .env.local 再填入真正的值。
# .env.local 已被 .gitignore 忽略，不會進版控。
#
# 本機測試 /api/consultation 要用 `vercel dev` 啟動，
# 因為 `npm run dev`（Vite）不會執行 /api 底下的 serverless function。

# Resend API Key，在 https://resend.com/api-keys 產生
RESEND_API_KEY=

# 要收表單通知的信箱
CONSULTATION_TO_EMAIL=

# Google Analytics 4 的評估 ID，格式是 G-XXXXXXXXXX
# 在 GA 後台「管理 → 資料串流 → 網站」建立資料串流後可以看到。
# 這個值會被打包進前端，本來就是公開的，不是機密。
# 留空 = 完全不載入 GA（本機開發預設就留空，才不會污染正式報表）。
VITE_GA_ID=
